torch>=2.3.0
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.9.6
accelerate>=0.33.0
bitsandbytes>=0.43.0

# Install after the above — vLLM pins its own torch/cuda build and can
# conflict if installed in the same resolver pass on some environments.
# vllm>=0.5.4
